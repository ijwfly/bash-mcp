services:
  bash-mcp:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./workspace:/workspace
    environment:
      - MCP_PORT=8080
      - BASH_TIMEOUT_MAX=300
      - JWT_SECRET=${JWT_SECRET:-}
      - ALLOW_NO_AUTH=${ALLOW_NO_AUTH:-}
